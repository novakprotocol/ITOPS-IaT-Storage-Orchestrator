# AI Contract Resources

- `template-contract.json` — machine-readable GHE Pages and source-of-truth rules.
- `document-profiles.json` — 22 profile definitions mapped to 32 controlled modules.
- `derived-document-intake.schema.json` — standard input brief for an AI or generator.
- `derived-document-intake.example.json` — synthetic, non-operational example input.

Human-readable instructions begin in `AGENTS.md`. The canonical module source is `section-library/2210-master.md`. When human-readable and machine-readable requirements conflict, stop and report the conflict instead of silently choosing one.
